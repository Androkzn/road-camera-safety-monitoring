# Weekend Improvements — Apr 18–20, 2026

42 commits across three days (Sat Apr 18 → Mon Apr 20). Totals: ~830k LoC net delta
(most of which is the one-time `node_modules/` / lockfile scrub in `015d22b`).
Meaningful code delta ≈ +20k / −6k across backend + frontend.

Themes, in roughly the order they landed:

1. Repo hygiene + launcher polish
2. Settings Console (new runtime-tuning surface)
3. Multi-source live grid + per-slot detection control
4. Video transport (MJPEG vs. polling) + decoder capping
5. Frontend restructure (TanStack Query, feature folders)
6. Video ↔ GPS playback sync + per-camera calibration
7. Shadow-mode Validator + drift surfacing
8. Events UX (detail dialog, low-risk toggle, clip rendering)
9. Encoding / redaction polish

---

## 1. Repo hygiene + launcher (Sat morning)

- [14fdc65](../../) — [start.py](../../start.py) rewritten as a one-command launcher: builds frontend → runs pytest → boots uvicorn → waits for `/api/live/status` → opens browser.
- [015d22b](../../), [2a1ff91](../../), [891404e](../../), [d6f49d2](../../) — removed committed `node_modules/`, `package-lock.json`, old `audit.jsonl` / `watchdog.jsonl`, static admin/index HTML and the dead FNOL module. `.gitignore` expanded to cover `tsbuildinfo`, node_modules, runtime logs, local Claude configs.
- [d6f49d2](../../) — server now fails loudly when `frontend/dist/` is missing instead of silently mounting nothing.

## 2. Settings Console (Sat afternoon) — biggest new feature

Runtime-tuning UI at [/settings](../../frontend/src/pages/SettingsPage.tsx), admin-bearer-gated.

- [20eeb82](../../) — schema + effective-settings + validation types, server routes, `ImpactMonitor` service.
- [2717a0c](../../) — public route serving the SPA shell; data endpoints stay admin-tier.
- [3391eb3](../../), [383743e](../../) — layout, help popovers, `humanizeKey()`, numeric-tunable widgets.
- [5f9954c](../../) — admin-token hooks clear the token on 401/403/503 and surface a reconnect banner.
- [d2cbf78](../../) — `confirm()`/`alert()` replaced with a dialog component; privacy-sensitive changes get a detailed confirm.
- [1d92da5](../../) — `psutil` added; failed applies **don't** burn the cooldown; `TARGET_FPS` now warm-reloads without restart.
- [2d3ad39](../../) — apply/rollback/template actions surface a diff + success banner + structured console log.
- [8844a10](../../) — `OpsDeltas` component shows before/after FPS + CPU after each apply; focused source ID persists across navigations via `localStorage`.
- [e2832b5](../../) — `ImpactCard` last-updated timestamp; polling 15 s → 5 s.

## 3. Multi-source grid + per-slot detection toggle (Sat evening)

- [83a6d31](../../) — `ROAD_STREAM_SOURCES` env drives N parallel `StreamSlot`s; `/api/live/sources` returns per-slot status; matching TS types on the frontend.
- [df478f2](../../) — per-slot `detection_enabled` toggle (pause detection without killing the feed); [AdminPage](../../frontend/src/pages/AdminPage.tsx) now renders [MultiSourceGrid](../../frontend/src/components/admin/MultiSourceGrid.tsx) instead of one big video.
- [6428bd6](../../) — YOLO auto-picks CUDA → MPS → CPU; big CPU drop in multi-source demos.
- [286a492](../../), [35cd4b8](../../), [b4bd667](../../), [742fa91](../../), [cc0e2e1](../../) — optimistic running-state updates, typed `BusyAction` (`starting` / `pausing` / `removing`), 8 s stuck-busy escape hatch, error-dialog on failure.

## 4. Video transport + decoder capping (Sat late / Sun)

- [79a50f3](../../), [da2e452](../../) — `StreamSlot` tracks active MJPEG subscribers **and** recent polls; frame encoding only runs when someone is watching. New single-shot `GET /admin/frame/{id}`.
- [7f938da](../../) — [StreamImage](../../frontend/src/components/admin/StreamImage.tsx) picks transport by page protocol: HTTPS → MJPEG, HTTP → polling. Ships with the deploy note (≥6 tiles needs HTTP/2 proxy). Also documented in [CLAUDE.md](../../CLAUDE.md).
- [f15b221](../../) — auto-shedding policy on the backend; frontend caps concurrent MJPEG decoders; [DetectionsPanel](../../frontend/src/components/admin/) and [HealthStrip](../../frontend/src/components/admin/) removed (dead code).

## 5. Frontend restructure (Sun afternoon)

- [de564f1](../../) — adopted **TanStack Query** for data fetching; moved to a feature-based folder layout; added "Start all" / "Pause all" bulk controls; new `/api/validator/status` endpoint.
- [383743e](../../) — watchdog destructive actions routed through `adminFetch` instead of raw `fetch`.

## 6. Video ↔ GPS sync + per-camera calibration (Sun evening)

- [19529eb](../../), [19e0998](../../) — [VehicleMap](../../frontend/src/components/VehicleMap.tsx) takes a `clock` prop; `mapClockSource` in AdminPage picks the running dashcam file as the authoritative clock; `videoKey` fetches a GPS track synced to the current clip; `syncHint` covers the "no GPS samples yet" state. `location-history.json` dropped in favor of GPX.
- [dfbfdd8](../../) — `.env.example` + README documented **per-slot camera calibration** (multi-camera vehicles); [AdminPage](../../frontend/src/pages/AdminPage.tsx) tracks authoritative playback position; detection distance overlays on rendered frames.

## 7. Shadow-mode Validator + drift surfacing (Sun night)

- [39aa350](../../) — [EventsPanel](../../frontend/src/components/) (detections + shadow-only findings) and [ValidatorControl](../../frontend/src/components/) (toggle the heavy shadow validator); hooks for validator status.
- [2721e82](../../) — `ValidatorControl` shows episodes-enqueued + overflow-depth metrics.
- [aa763a0](../../) — [ValidationPage](../../frontend/src/pages/) route; `useDriftCount` hook surfaces shadow-validator drift on Admin / Dashboard / Settings.
- [dc4ddf9](../../) — `ValidatorWorker` logs richer error context on job failure.

## 8. Events UX + clip rendering (Sun night → Mon)

- [0d8b697](../../), [2721e82](../../) — "Clear events" in AdminPage + DashboardPage + WatchdogDrawer (with confirm dialog); backend API endpoint does a soft wipe of the in-memory buffer. `EventCard` skip-reason messaging cleaned up (ALPR-enrichment skips show something actionable instead of "N/A").
- [f1707e9](../../) — **Show-low-risk toggle** across AdminPage / DashboardPage / MonitoringPage / EventsPanel. Filtering logic unified.
- [e94ea3b](../../) — [AdminEventCard](../../frontend/src/components/) + `EventCard` accept `onSelect` → cards are now buttons that open the event detail dialog (keyboard focus + pointer cursor + focus outlines). Backend **clip rendering**: annotated video clips generated for review.

## 9. Encoding / redaction polish (Mon morning)

- [b14efd6](../../) — explicit MP4 muxer flag in the server's video encoder (was tripping on file-suffix inference); `redact.py` thickens context-detection rectangles so they survive JPEG-compression artifacts in thumbnails.

---

## Shipping posture

- Three privacy/auth invariants preserved: plate scrub at ingest, dual-thumbnail output,
  admin-tier endpoints behind `require_bearer_token()`.
- Conflict-detection gates **untouched** — this weekend was infrastructure, UI, and
  shadow-mode work, not perception changes.
- Tests: suite is expected to still be green (no gate loosening; `start.py` still runs
  pytest before boot). Re-run `make test` before demo.

## Still open / not in this window

- Clip-rendering endpoint exists but there is no UI affordance yet to download a clip.
- Auto-shedding policy is committed but unproven at ≥8 tiles — needs a load test.
- Per-slot calibration is documented; no calibration UI yet (still env-driven).
