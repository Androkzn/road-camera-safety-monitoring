/**
 * settings feature — public surface.
 *
 * Only SettingsPage is exposed; the Tunable primitives, column layout,
 * and step-computation util stay feature-private. App shell imports from here.
 */
export { SettingsPage } from "./SettingsPage";
