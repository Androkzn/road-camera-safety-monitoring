"""Test package marker for the Road Safety pytest suite.

This file is intentionally almost empty. Its presence turns ``tests/`` into a
Python package, which lets pytest collect the ``test_*.py`` modules beside it
and lets ``conftest.py`` share fixtures across every test file.
"""
