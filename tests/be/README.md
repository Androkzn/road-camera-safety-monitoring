# Backend tests

Backend test suites live in `tests/` (Python/pytest).

This folder marks the FE/BE grouping convention:

- `tests/be` for backend-focused test organization
- `tests/fe` for frontend-focused test organization
